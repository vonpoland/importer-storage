{
  "name": "importer-storage-stack",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "npx cdk --app \"npx ts-node bin/app.ts\" deploy --all --require-approval never",
    "destroy": "npx cdk --app \"npx ts-node bin/app.ts\" destroy --all --force"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.826.0",
    "@aws-sdk/client-s3": "^3.826.0",
    "aws-cdk": "^2.1033.0",
    "aws-cdk-lib": "^2.200.1",
    "aws-lambda": "^1.0.7",
    "axios": "^1.9.0",
    "constructs": "^10.4.2",
    "dotenv": "^16.5.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
